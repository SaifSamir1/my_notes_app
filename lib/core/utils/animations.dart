class AppAnimationsDuration {
  const AppAnimationsDuration();

  static Duration get defaultDuration => const Duration(
        milliseconds: 500,
      );

  static Duration get circularProgressIndicator => const Duration(
        milliseconds: 450,
      );
}
